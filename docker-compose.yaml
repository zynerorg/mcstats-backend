services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 2456:80
    volumes:
      - ./data/world:/app/data/world
      - ./data/usercache.json:/app/data/usercache.json
    restart: unless-stopped
